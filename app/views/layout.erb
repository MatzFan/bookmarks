<!DOCTYPE html>
<html>
<head>
  <title>Bookmark manager</title>
  <link rel="stylesheet" type="text/css" href="/css/application.css">
  <body>
    <% if current_user %>
      Welcome, <%= current_user.email %>
    <% end %>
    <%= yield %> <!-- yields to erb code -->
  </body>
</head>
</html>
